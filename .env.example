# Required for /admin to be reachable
ADMIN_USER=changeme
ADMIN_PASSWORD=changeme

# 32+ random bytes — generate with: openssl rand -hex 32
SESSION_SECRET=changeme-with-output-of-openssl-rand-hex-32

# Optional — overrides the default data dir
# SITE_DATA_DIR=/data
